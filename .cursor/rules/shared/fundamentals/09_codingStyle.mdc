---
description:
globs:
alwaysApply: false
---

Use shorthands where possible. Examples include:
- object structuring / destructuring
- .length check `if (x.length)` is better than `if (x.length > 0)`
Prefer expressions. Use the ternary operator, but no nested ternaries.
